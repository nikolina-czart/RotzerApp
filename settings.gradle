pluginManagement {

    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        flatDir {
            dirs 'libs'
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soo

        // e.g this is how you would add jitpack
        maven { url "https://jitpack.io" }

        flatDir {
            dirs 'libs'
        }

    }
}
rootProject.name = "RotzerApp"
include ':app'
